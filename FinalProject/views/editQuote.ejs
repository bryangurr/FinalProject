<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Quote</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Edit Quote</h1>
    
    <form action="/editQuote/<%= quoteInfo.quoteid %>" method="post">
        <label for="quotedescription">Description:</label>
        <textarea id="quotedescription" name="quotedescription" required><%= quoteInfo.quotedescription %></textarea>

        <label for="state">State:</label>
        <input type="text" id="state" name="state" value="<%= quoteInfo.state %>" required>

        <label for="county">County:</label>
        <input type="text" id="county" name="county" value="<%= quoteInfo.county %>" required>

        <label for="locationrate">Location Rate:</label>
        <input type="number" step="0.01" id="locationrate" name="locationrate" value="<%= quoteInfo.locationrate %>" required>

        <label for="quoteyear">Quote Year:</label>
        <input type="number" id="quoteyear" name="quoteyear" value="<%= quoteInfo.quoteyear %>" required>

        <label for="meansurviverate">Mean Survive Rate:</label>
        <input type="number" step="0.01" id="meansurviverate" name="meansurviverate" value="<%= quoteInfo.meansurviverate %>" required>

        <label for="curryearseed">Current Year Seed:</label>
        <input type="number" step="0.01" id="curryearseed" name="curryearseed" value="<%= quoteInfo.curryearseed %>" required>

        <label for="cappedyield">Capped Yield:</label>
        <input type="number" step="0.01" id="cappedyield" name="cappedyield" value="<%= quoteInfo.cappedyield %>" required>

        <label for="priceelectionper">Price Election Per:</label>
        <input type="number" step="0.01" id="priceelectionper" name="priceelectionper" value="<%= quoteInfo.priceelectionper %>" required>

        <label for="expcommodvalue">Expected Commodity Value:</label>
        <input type="number" step="0.01" id="expcommodvalue" name="expcommodvalue" value="<%= quoteInfo.expcommodvalue %>" required>

        <fieldset>
            <legend>Creator Information</legend>
            <p>First Name: <%= quoteInfo.firstname %></p>
            <p>Last Name: <%= quoteInfo.lastname %></p>
        </fieldset>

        <button type="submit">Update Quote</button>
    </form>

    <a href="/quotes">Back to Quotes List</a>
</body>
</html>
